#include <iostream>
#include <vector>
#include <climits>

using namespace std;

/**
 * (beginner)
 * @param v - coin values
 * @param s - sum
 */
int minCoins(vector<int> v, int s) {
    // TODO
    return -1;
}

/**
 * (elementary)
 */

int main() {
    // minCoins
    vector<int> coinValues = {1, 3, 5};
    int sum = 11;
    cout << minCoins(coinValues, sum) << endl;
}