#include <algorithm>
#include <climits>
#include <cmath>
#include <iostream>
#include <unordered_map>
#include <vector>

using namespace std;


int main()
{
	int n, x;
	cin >> n >> x;
	vector<int> arr(n, 0);
	int min = INT_MAX, min_index = 0;

	for (int i=0; i<n; i++) {
		cin >> arr[i];
		if (arr[i] < min) {
			
		}
	}
	cout << min << endl;
	// int all = pos * i + 

	// for (int i =0;i<start.size();i++) {
	// 	cout << start[i] << " " << endl;
	// }
}