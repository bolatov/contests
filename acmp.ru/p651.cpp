#include <bits/stdc++.h>

using namespace std;

#ifndef int64
#define int64 long long
#endif

vector<int64> p;

int main() {
    int64 N, M;
    cin >> N >> M;
    int64 n = min(N, M);
    int64 m = max(N, M);
    // TODO

    return 0;
}