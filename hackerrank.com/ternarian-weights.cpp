#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

#ifndef int64
#define int64 long long
#endif

int main() {
    int64 t;
    cin >> t;
    vector<int64> v;
    for (int i = 1; i < 1e9 + 7; i *= 3) {
        v.push_back(i);
    }
    while (t--) {
        int n;
        cin >> n;
    }
    return 0;
}
