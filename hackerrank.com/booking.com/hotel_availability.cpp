#include <bits/stdc++.h>

using namespace std;

// Hotel --> How many rooms it has
unordered_map<string, int> hotel2rooms;

// Hotel Bookings
// Hotel --> {check-in date, check-out date}
unordered_map<string, pair<string, string>> hotel2checkInOut;
vector<string> availableHotels(string checkInDate, checkOutDate) {
    vector<string> v;
}

int main() {
    return 0;
}